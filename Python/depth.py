# what is depth first search? 
# this is a sorting algorithm, mainly on graphs and trees subsequently, that takes one route and follows through until it reaches a dead end or visits a node 
# it has already visited, afterwards go back to a previously traveled node that has another path and continues until that path reaches a dead end or a already 
# visited node and repeat
# uses stacks

def dfs():
    pass

def dfsHelper():
    pass

